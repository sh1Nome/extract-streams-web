services:
  python:
    container_name: "python_service"
    build:
      context: "./python"
    init: true
    volumes:
      - "./python/apisource:/app"
    ports:
      - "8000:8000"
    environment:
      - "TZ=Asia/Tokyo"
  node:
    image: "node:23-slim"