services:
  python:
    build: "./python"
    init: true
    volumes:
      - "./python/apisource:/home/work/apisource"
    ports:
      - "8000:8000"
    command: ["./.local/bin/uvicorn", "apisource.main:app", "--host", "0.0.0.0", "--reload"]
  node:
    image: "node:23-slim"